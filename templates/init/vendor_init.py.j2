"""Vendored upstream research code package marker.

This directory is populated by `just fetch <model>` / `just setup <model>`.
It is included in sdists/wheels so PyPI/HuggingFace installs can import vendored code.
"""

